package org.example.morgan;

public class MessageQueue {
    /**
     * topic -> publisher, subscriber
     *
     *
     * Broker
     * topic -> partitions
     * class Publisher{
     *     private KafkaTemplate<K, V>;
     * }
     * publisher -> Template -> pushMessage -> partition
     * Consumer Groups (consumer) -> consume -> listening to topic ( partition )
     *
     *
     * **/
}
